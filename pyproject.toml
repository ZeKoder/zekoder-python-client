[tool.poetry]
name = "zepyclient"
version = "0.1.0"
description = "Python client for REST Endpoints generated by ZeKoder"
authors = ["AhmedBytesBits <ahmed@cyberneticlabs.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
